models:
  - name: bronze_bookings #  Must match the filename of a model -- including case sensitivity.
    config:
      materialized: table
  - name: bronze_hosts #  Must match the filename of a model -- including case sensitivity.
    config:
      materialized: table
  - name: bronze_listings #  Must match the filename of a model -- including case sensitivity.
    config:
      materialized: table